using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectPool : Singleton<ObjectPool> {
    // TODO!! If object needs to be instantiated it should be taken from
    // object-pool, and if object needs to be destroyed it should be moved to
    // the pool.
}
